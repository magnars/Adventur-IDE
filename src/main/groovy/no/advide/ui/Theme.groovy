package no.advide.ui

import java.awt.Color
import javax.swing.JPanel

class Theme {

  /**
  // papir
  static panel = new BackgroundPanel()
  static headerText = new Color(150, 150, 140)

  static prose = new Color(0, 0, 0)
  static command = new Color(90, 90, 80)
  static prefix = prose

  static cursorHighlight = new Color(0, 0, 0, 4)
  static cursor = Color.black

  static roomExists = command
  static roomDoesntExist = Color.red
  static roomHighlight = new Color(0, 0, 150, 20)

  static brackets = command
  static alternatives = prose
  static altArrow = new Color(0, 0, 0, 60)
  static separatorLine = new Color(150, 150, 100, 30)

  static embossedTop = new Color(0, 0, 0, 30)
  static embossed = new Color(0, 0, 0, 7)
  static embossedBottom = new Color(255, 255, 255, 150)

  static defaultFallbackColor = command
  /**/

  /**/
  // svart
  static panel = new JPanel(background: Color.black)
  static headerText = new Color(90, 90, 90)

  static prose = new Color(150, 150, 150);
  static command = new Color(90, 90, 90)
  static prefix = prose

  static cursorHighlight = new Color(0, 0, 0, 0)
  static cursor = Color.white

  static roomExists = command
  static roomDoesntExist = new Color(210, 30, 30)
  static roomHighlight = new Color(30, 30, 70)

  static brackets = command
  static alternatives = prose
  static altArrow = new Color(60, 60, 60)
  static separatorLine = new Color(60, 60, 60)

  static embossedTop = new Color(20, 20, 50)
  static embossed = new Color(100, 100, 255, 25)
  static embossedBottom = embossedTop

  static defaultFallbackColor = command
  /**/

  /*
  // gamle editor
  final static Color COL_BACKGROUND = Color.black;
  final static Color COL_FOREGROUND = new Color(150, 150, 150);

  final Color COL_KOMMANDO = new Color(90, 90, 90);
  final Color COL_DELKOMMANDO = new Color(120, 120, 120);
  final Color COL_ANTALL_ALTERNATIVER = new Color(150, 150, 150);
  final Color COL_ALTERNATIVER = new Color(130, 130, 130);
  final Color COL_UTILGJENGELIG = new Color(210, 30, 30);
  final Color COL_TING = new Color(255, 255, 255);
  final Color COL_BRUKT = new Color(100, 100, 210);
  final Color COL_UKJENT = new Color(210, 30, 30);
  final Color COL_COMMENT = new Color(60, 60, 160);
  final Color COL_SPESIELL = new Color(170, 170, 255);
  final Color COL_ANNET_EVENTYR = new Color(210, 100, 210);
  final Color COL_TYPO = new Color(255, 90, 90);
  final Color COL_ACTIVE_TYPO = new Color(255, 130, 130);
  final Color COL_HIGHLIGHT = new Color(255, 255, 130);
  */

}
